These markdown files are generated using `pandoc` and `git`.

On Ubuntu, you can install them using:

```
$ sudo apt install pandoc git
```

And then generate all markdown files using:

```
$ ./generate_peps.sh
```

