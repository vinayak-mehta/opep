These markdown files are generated using `pandoc`.

On Ubuntu, you can install it using:

```
$ sudo apt install pandoc
```

And then generate all markdown files using:

```
$ ./generate_peps_md.sh
```

